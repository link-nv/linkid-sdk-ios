#include "Pods-Base64nl.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/Base64nl" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/Base64nl"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}